describe("Backend Test Infrastructure", () => {
  test("Jest is configured correctly", () => {
    expect(true).toBe(true);
  });

  test("Basic arithmetic works", () => {
    expect(2 + 3).toBe(5);
  });

  test("Strings are equal", () => {
    expect("LeaveFlow").toBe("LeaveFlow");
  });
});