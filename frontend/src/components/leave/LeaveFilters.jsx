import Button from "../ui/Button";
export default function LeaveFilters({ filters, setFilters, scope, types }) { const update = (key, value) => setFilters({ ...filters, [key]: value, page: 1 }); return <form className="grid gap-3 rounded-xl border border-slate-200 bg-white p-4 sm:grid-cols-2 xl:grid-cols-5" onSubmit={(event) => event.preventDefault()}><label className="text-sm font-medium">Status<select className="mt-1.5 w-full rounded-lg border border-slate-300 p-2" value={filters.status} onChange={(event) => update("status", event.target.value)}><option value="">All</option><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option><option value="cancelled">Cancelled</option></select></label><label className="text-sm font-medium">Leave type (loaded results)<select className="mt-1.5 w-full rounded-lg border border-slate-300 p-2" value={filters.leaveType} onChange={(event) => update("leaveType", event.target.value)}><option value="">All</option>{(types || []).map((type) => <option value={type._id} key={type._id}>{type.name}</option>)}</select></label><label className="text-sm font-medium">Start date<input type="date" className="mt-1.5 w-full rounded-lg border border-slate-300 p-2" value={filters.startDate} onChange={(event) => update("startDate", event.target.value)} /></label><label className="text-sm font-medium">End date<input type="date" className="mt-1.5 w-full rounded-lg border border-slate-300 p-2" value={filters.endDate} onChange={(event) => update("endDate", event.target.value)} /></label>{scope === "organization" && <label className="text-sm font-medium">Employee ID<input className="mt-1.5 w-full rounded-lg border border-slate-300 p-2" placeholder="Optional MongoDB ID" value={filters.userId} onChange={(event) => update("userId", event.target.value)} /></label>}<label className="text-sm font-medium">Search loaded results<input className="mt-1.5 w-full rounded-lg border border-slate-300 p-2" value={filters.search} onChange={(event) => update("search", event.target.value)} placeholder="Employee or reason" /></label><Button variant="secondary" className="self-end" onClick={() => setFilters({ page: 1, limit: 20, status: "", leaveType: "", startDate: "", endDate: "", userId: "", search: "" })}>Clear filters</Button></form>; }
