import { Building2, CalendarDays, ClipboardCheck, ListChecks, Users } from "lucide-react";
import { useNavigate } from "react-router-dom";
import Card from "../ui/Card";
const actions = [{ label: "Apply Leave", icon: CalendarDays, to: "/leave/apply" }, { label: "Mark Attendance", icon: ClipboardCheck, to: "/attendance/mark" }, { label: "View Attendance", icon: ListChecks, to: "/attendance/history" }, { label: "View Leave History", icon: CalendarDays, to: "/leave/history" }, { label: "Employee Directory", icon: Users, to: "/employees" }, { label: "Departments", icon: Building2, to: "/departments" }];
export default function QuickActions({ role }) { const navigate = useNavigate(); const visible = actions.filter((action) => !["Employee Directory", "Departments"].includes(action.label) || ["hr", "admin"].includes(role)); return <Card className="p-5"><h2 className="font-semibold text-slate-900">Quick Actions</h2><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3">{visible.map(({ label, icon: Icon, to }) => <button key={label} onClick={() => navigate(to)} className="rounded-xl border border-slate-200 p-3 text-left transition hover:border-teal-300 hover:bg-teal-50"><Icon size={20} className="text-teal-700" /><span className="mt-3 block text-sm font-medium text-slate-700">{label}</span></button>)}</div></Card>; }
