import { ChevronLeft, ChevronRight } from "lucide-react";
import Button from "../ui/Button";
import AttendanceStatusBadge from "./AttendanceStatusBadge";
export default function AttendanceCalendar({ month, onMonthChange, records, onSelect }) { const year = month.getFullYear(); const monthIndex = month.getMonth(); const firstDay = new Date(year, monthIndex, 1).getDay(); const days = new Date(year, monthIndex + 1, 0).getDate(); const byDay = new Map(records.map((record) => [new Date(record.date).getDate(), record])); const cells = Array.from({ length: firstDay + days }, (_, index) => index < firstDay ? null : index - firstDay + 1); return <section className="rounded-xl border border-slate-200 bg-white p-4 sm:p-5"><div className="mb-5 flex items-center justify-between"><Button variant="ghost" className="px-2" onClick={() => onMonthChange(new Date(year, monthIndex - 1, 1))} aria-label="Previous month"><ChevronLeft /></Button><h2 className="font-semibold text-slate-900">{month.toLocaleDateString(undefined, { month: "long", year: "numeric" })}</h2><Button variant="ghost" className="px-2" onClick={() => onMonthChange(new Date(year, monthIndex + 1, 1))} aria-label="Next month"><ChevronRight /></Button></div><div className="grid grid-cols-7 gap-px overflow-hidden rounded-lg border border-slate-200 bg-slate-200">{["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"].map((day) => <div className="bg-slate-50 p-2 text-center text-xs font-semibold text-slate-500" key={day}>{day}</div>)}{cells.map((day, index) => { const record = day && byDay.get(day); const weekend = day && [0, 6].includes(new Date(year, monthIndex, day).getDay()); return <button key={index} disabled={!record} onClick={() => record && onSelect(record)} className={`min-h-20 bg-white p-2 text-left align-top transition sm:min-h-24 ${record ? "hover:bg-teal-50 focus:bg-teal-50" : "cursor-default"} ${weekend ? "bg-slate-50" : ""}`}><span className={`text-xs font-medium ${weekend ? "text-slate-400" : "text-slate-700"}`}>{day}</span>{record && <span className="mt-2 block"><AttendanceStatusBadge status={record.status} /></span>}{weekend && !record && <span className="mt-2 block text-[10px] text-slate-400">Weekend</span>}</button>; })}</div><p className="mt-4 text-xs text-slate-500">Select a recorded day to view attendance details. Weekends and statuses are shown only when supported by attendance records.</p></section>; }
