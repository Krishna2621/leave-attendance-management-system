import { Eye, Pencil } from "lucide-react";
import Table from "../common/Table";
import Button from "../ui/Button";
import AttendanceStatusBadge from "./AttendanceStatusBadge";
import { formatDate, formatHours, formatTime } from "../../utils/attendance";
export default function AttendanceTable({ records, scope, onDetails, onCorrect, sort, onSort }) { const sortLabel = (label, key) => <button className="font-semibold hover:text-teal-700" onClick={() => onSort(key)}>{label}{sort.key === key ? (sort.direction === "asc" ? " ↑" : " ↓") : ""}</button>; const columns = [{ key: "date", label: sortLabel("Date", "date"), render: (row) => formatDate(row.date) }, ...(scope !== "my" ? [{ key: "employee", label: sortLabel("Employee", "employee"), render: (row) => row.employee?.name || "—" }] : []), { key: "status", label: sortLabel("Status", "status"), render: (row) => <AttendanceStatusBadge status={row.status} /> }, { key: "punchIn", label: "Check-in", render: (row) => formatTime(row.punchIn) }, { key: "punchOut", label: "Check-out", render: (row) => formatTime(row.punchOut) }, { key: "hoursWorked", label: "Hours", render: (row) => formatHours(row.hoursWorked) }, { key: "actions", label: "Actions", render: (row) => <div className="flex gap-1"><Button variant="ghost" className="px-2 py-2" onClick={() => onDetails(row)} aria-label="View attendance details"><Eye size={17} /></Button>{onCorrect && <Button variant="ghost" className="px-2 py-2" onClick={() => onCorrect(row)} aria-label="Correct attendance"><Pencil size={17} /></Button>}</div> }]; return <Table columns={columns} data={records} keyField="_id" emptyMessage="No attendance records found for these filters." />; }
