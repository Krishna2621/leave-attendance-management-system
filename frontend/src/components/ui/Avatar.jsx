export default function Avatar({ name = "User", src, className = "" }) { return src ? <img src={src} alt={name} className={`size-9 rounded-full object-cover ${className}`} /> : <span className={`inline-flex size-9 items-center justify-center rounded-full bg-teal-100 text-sm font-bold text-teal-800 ${className}`}>{name.trim().split(/\s+/).slice(0, 2).map((part) => part[0]).join("").toUpperCase()}</span>; }
