import { useLeaveBalances, useLeaveRequests } from "../../hooks/useLeave";
import Loader from "../../components/ui/Loader";
import Card from "../../components/ui/Card";
import DashboardError from "../../components/dashboard/DashboardError";
export default function LeaveBalancePage() { const balances = useLeaveBalances(); const pending = useLeaveRequests("my", { status: "pending", page: 1, limit: 1 }); if (balances.isLoading || pending.isLoading) return <Loader label="Loading leave balances…" />; if (balances.isError) return <DashboardError error={balances.error} onRetry={balances.refetch} />; return <div className="mt-6"><h1 className="text-2xl font-bold">Leave Balance</h1><p className="mt-1 text-sm text-slate-600">Available, used, remaining, and pending leave for {balances.data?.year}.</p><p className="mt-4 inline-flex rounded-full bg-amber-50 px-3 py-1.5 text-sm font-semibold text-amber-800">Pending leave requests: {pending.data?.pagination?.totalRecords || 0}</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{(balances.data?.leaveBalances || []).map((item) => <Card className="p-5" key={item._id}><h2 className="font-semibold">{item.leaveTypeId?.name}</h2><dl className="mt-4 space-y-2 text-sm"><div className="flex justify-between"><dt className="text-slate-500">Available</dt><dd>{item.allocated}</dd></div><div className="flex justify-between"><dt className="text-slate-500">Used</dt><dd>{item.used}</dd></div><div className="flex justify-between font-semibold text-teal-700"><dt>Remaining</dt><dd>{item.remaining}</dd></div></dl></Card>)}</div>{!balances.data?.leaveBalances?.length && <p className="mt-6 text-sm text-slate-500">No leave balances are available.</p>}</div>; }
